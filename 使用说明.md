# ShipSolver 使用说明

## 基本操作

- 左键（点按）标记"船"，再点一次取消；
- 右键（长按）标记"空"，再点一次取消；
- Shift + 左键：选中整艘船；
- Ctrl + Z：撤销（可连续撤销）。

## 探测辅助

- **探测建议**：面板给出推荐探测点（前 3），含覆盖数、命中率；手机上推荐标号直接画在棋盘格子上；
- **大探测**：开始大探测后连续录入结果（船/空），确认时统一写入；
- **必然结论**：能 100% 判定的格子自动确认（必为船/必为空），高置信格（>60%）金色提示；
- **热力图**：棋盘常态显示相对船概率渐变（蓝→红），悬停/点按查看精确百分比。

## 确认整船

标出船格后点「确认整船」，长度匹配且两端封闭即可确认；也可右键船格快捷确认。

## 存档

- 自动保存当前进度，重启后恢复；
- 命名保存、分期管理（设置或存读档弹窗切换）；
- 「导出全部」备份所有期，「导入」恢复；
- 每盘完成自动归档，误删有确认提示，损坏存档自动备份。

## 快捷键

- Ctrl + Z：撤销
- Ctrl + Shift + R：紧急重置（清空所有数据并刷新）

## 手机 / 平板

- 点击 = 标船，长按约 0.5 秒 = 标空（长按可能触发浏览器菜单时可改用「标船/标空」切换按钮）；
- 棋盘四角/快捷条提供撤销、切换、确认整船、大探测快捷按钮；
- 设置里可调整：四角按钮开关、坐标号显示、排版模式（自动/强制电脑/强制手机）。
