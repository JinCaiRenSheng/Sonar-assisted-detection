# ShipSolver 使用说明（一步一步教你用）

这个工具是给《战舰》类游戏（海岛奇兵 Boom Beach 也适用）用的"推理小助手"：你把游戏里探到的结果（船/空）记进来，它帮你分析接下来最该探哪里。**单文件、不用安装、不用联网。**

---

## 〇、使用前提：断网玩法（安卓双机）

这个游戏需要**断网**才能玩，而工具只在你手上帮你记格子，所以需要两台手机配合：

1. **手机 A（开热点）**：打开"个人热点"；
2. **手机 B（游戏机）**：连接手机 A 的热点；
3. 手机 B 打开游戏后，**手机 A 关掉自己的数据连接**（注意：是关数据，不是关热点）——这样手机 B 虽然连着 WiFi，但已经断外网，满足游戏断网要求；
4. 手机 B 在游戏里探测，把结果录进本工具；
5. 一次操作完成后，**在手机 B 上划掉游戏进程**，手机 A 重新打开数据连接，手机 B 重进游戏；
6. 重复第 3~5 步，一轮一轮推进。

> 核心就一句：**游戏机永远连热点 WiFi，热点机负责"断开/打开"外网数据**，游戏机不用碰网络设置。

---

## 一、先把它打开

### 在电脑上

1. 把 `index.html` 这个文件下载到电脑上（放桌面最方便）；
2. 对着文件**双击**；
3. 它会用浏览器打开（推荐 Chrome 或 Edge），看到蓝色界面就成功了。

> 如果双击后没有打开，而是弹出"选择打开方式"，选"浏览器"（Chrome/Edge）就行。

### 在手机上

1. 把 `index.html` 这个文件发到手机（微信"文件传输助手"、QQ、网盘、数据线都行）；
2. 在手机的文件管理里找到它；
3. **长按它** → 选"打开方式"（或"用其他应用打开"）→ 选浏览器（Chrome、夸克、自带浏览器都行）。

> 如果点开只看到一堆文字而不是彩色界面，说明是用"文档查看器"打开的。别急：回到文件管理，长按文件 → "打开方式" → 选浏览器，就会变成正常的工具界面。

---

## 二、打开之后，你看到什么（电脑版）

从上往下看：

- **最上面**：标题和一行按钮（撤销、棋盘与配置、重置、存读档、资源、设置、反馈、帮助）；
- **中间**：大棋盘（格子是菱形排列的），左边有一列小色块（图例）；
- **右边**：几个信息面板——进度与状态、探测建议、探测操作；
- 手机和平板上布局会自动变化，但功能一样。

---

## 三、开始记录：怎么把"船"和"空"标上去

游戏里每探一个格子，都会告诉你结果是"船"还是"空"，回到工具里把它记下来：

### 电脑上（鼠标）

- **左键点格子 = 标"船"**（格子变红）；再点一次 = 取消；
- **右键点格子 = 标"空"**（格子变深色）；再点一次 = 取消。

### 手机上（手指）

- **点一下 = 标"船"**；
- **长按约半秒 = 标"空"**（长按时手机会轻轻震一下，代表成功）。

> 如果手机上长按老是弹出"复制"菜单，别用长按了：点界面上的「●标船 / ×标空」按钮切换，再点格子就行——按钮上写着当前要标什么，点一下换成"标空"，之后点格子都是空。

---

## 四、工具帮你分析：接下来探哪里

标完一批结果后，工具会自动分析，告诉你推荐探测点：

- **右侧"探测建议"面板**：列出推荐的前 1、2、3 个格子（带坐标，比如 (5,5)），还有"覆盖数"和"命中率"；
- **手机上**：推荐 1、2、3 的编号直接画在棋盘格子上，一看就知道往哪点；
- **热力图**：棋盘上蓝→橙的渐变，越偏橙越可能是船；
- **金色粗框格子**：高置信格，出现的可能性超过六成，优先探。

照着推荐点格子探，探完回来把结果标上，工具会继续更新建议——就这样一格格推进。

---

## 五、大探测怎么用

大探测是"一次探多个格子、最后统一记录"的模式：

1. 点「开始大探测」（在右侧"探测操作"面板，手机上在棋盘下方的快捷条）；
2. 然后每探一个格子，回工具点一下对应格子记录结果（点 = 船，长按 = 空）；
3. 全部探完，点「确认本次」，一次性写入盘面；
4. 中途不想要了，点「取消本次」。

---

## 六、确认整船

当你把一艘船的格子都标出来、确定它完整了：

- 点「确认整船」按钮（电脑在图例下方，手机在棋盘下方快捷条，大屏在棋盘四角🔒）；
- 或者电脑上**右键**点船格（只有两端都封闭时才会成功）。

确认后这艘船就锁定了，工具不会再往它的位置推荐。

---

## 七、存档：进度怎么保存、怎么换设备

### 自动保存（不用管）

工具每几秒自动保存一次，就算关掉页面，下次打开还是上次的进度。

### 手动存档

1. 点「💾存读档」；
2. 在"存档名称"里输入名字（比如"8月4日-战舰"）；
3. 点「保存」。

以后点「存读档」，就能看到存档列表，点「读取」继续玩。

### 换手机 / 换电脑

手机和电脑的存档是**分开**的，要搬家：

1. 在旧设备点「存读档」→「导出全部」，会下载一个文件；
2. 把这个文件发到新设备；
3. 新设备打开工具 →「存读档」→「导入」→ 选那个文件。

### 手滑改错了怎么办

- 按 `Ctrl + Z`（手机上点快捷条/顶栏的「↶撤销」）可以一步步退回；
- 刷新页面后撤销记录还在，不用怕。

---

## 八、设置里可以改什么

点「⚙️设置」，可以开关这些：

| 设置项 | 干什么 |
|---|---|
| 启用棋盘快捷按钮 | 大屏/平板棋盘四角显示快捷按钮（撤销、切换、确认整船、大探测） |
| 显示格子坐标号 | 格子上显示 (1,1) 这种编号；看不清可关掉 |
| 手机/电脑排版 | 自动 / 强制电脑排版 / 强制手机排版（一般用"自动"就行） |
| 显示概率热力图 | 棋盘颜色渐变提示，默认开 |
| 历史先验 | 用历史盘面规律辅助推荐，默认开 |

---

## 九、常见问题

**Q：手机上长按弹复制菜单？**
A：用「●标船 / ×标空」切换按钮代替长按，功能完全一样。

**Q：手机打开文件只看到文字？**
A：长按文件 → 打开方式 → 选浏览器，就会显示正常界面。

**Q：手机上工具栏/按钮找不到？**
A：手机界面是"单栏"：从上到下依次是按钮、棋盘、图例、面板。部分面板默认收起，点标题就能展开；常用操作在棋盘下方的快捷条。

**Q：电脑上棋盘区域滚轮滚不动页面？**
A：已修复，鼠标在棋盘上滚轮即可正常滚动页面。

**Q：存档怎么在手机和电脑之间互通？**
A：用"导出全部 → 发文件 → 导入"搬家（见第七节）。

**Q：界面太挤/太大？**
A：设置里"手机/电脑排版"选"强制手机排版"或"强制电脑排版"试试；大屏上四个角有快捷按钮，也可以关掉。

**Q：全部标完显示完成？**
A：说明所有船都确认了，剩下的格子工具会自动标空，游戏结束。

---

## 十、快捷键

- `Ctrl + Z`：撤销（手机上点「↶撤销」按钮）
- `Ctrl + Shift + R`：紧急重置（清空所有数据并刷新，慎用）

---

## 十一、问题反馈（用着不舒服就发日志）

日志是工具进化的燃料——用着卡、报错、或者哪里不对劲，都欢迎发过来。按下面操作，几秒钟的事：

1. 在工具里点右上角「📤反馈」；
2. 手机上会弹出一个窗口，点「📋复制全文」（或「⬇下载文件」）；
3. 把复制的内容粘贴到聊天里发给开发者（或把下载的文件发过去）；
4. 日志里包含版本号、操作记录、推理记录、卡顿数据（长任务/耗时）和统计，开发者靠它定位问题，**不含任何账号信息**。

> 卡顿时顺手导出一次，日志里的"长任务"和"耗时"能直接说明卡在哪一步。
